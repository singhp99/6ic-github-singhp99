{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to my test website!","text":""},{"location":"#introduction","title":"Introduction","text":"<p>Hello! My name's Pranjal and this is a test website that explores five different functions and implements the D\u00e9taxis method. If you would like to contact me please use the email singhp19@msu.edu for any questions.</p>"},{"location":"#the-detaxis-method","title":"The D\u00e9taxis Method","text":"<p>This method is a type of technical documentation approach that consists of dividing your website up into the following categories:</p> <ul> <li>Tutorials </li> <li>How-to</li> <li>Explanation</li> <li>Reference </li> </ul> <p>For more information visit the D\u00e9taxis website.</p>"},{"location":"adder/","title":"Adder Function","text":"<p>This function adds the inputs together</p> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>int | float</code> <p>any number</p> required <code>b</code> <code>int | float</code> <p>any number</p> required <code>c</code> <code>int | float</code> <p>any number</p> required <p>Returns:</p> Type Description <code>int | float</code> <p>float | int: sum of the inputs a, b and c</p> Source code in <code>example_functions.py</code> <pre><code>def my_adder(a: int | float, b: int | float, c: int | float) -&gt; int | float:\n    \"\"\"This function adds the inputs together\n\n    Args:\n        a (int | float): any number\n        b (int | float): any number\n        c (int | float): any number\n\n    Returns:\n        float | int: sum of the inputs a, b and c\n    \"\"\"\n\n    # this is the summation\n    out = a + b + c\n\n    return out\n</code></pre>"},{"location":"area/","title":"Area of Rectangle","text":"<p>This function returns the area of a rectangle with a width and height</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>int | float</code> <p>width of the rectangle</p> required <code>height</code> <code>int | float</code> <p>height of the rectangle</p> required <p>Returns:</p> Type Description <code>int | float</code> <p>int | float: area of the rectangle</p> Source code in <code>example_functions.py</code> <pre><code>def area_of_rectangle(width: int | float,height: int | float) -&gt; int | float:\n    \"\"\"This function returns the area of a rectangle with a width and height\n\n    Args:\n        width (int | float): width of the rectangle\n        height (int | float): height of the rectangle\n\n    Returns:\n        int | float: area of the rectangle\n    \"\"\"\n    area = width*height\n    return area\n</code></pre>"},{"location":"explanation_thermo/","title":"Themo Stat Function Explanation","text":"<p>Summarized from this page.</p>"},{"location":"explanation_thermo/#importance-of-thermostat-coding","title":"Importance of Thermostat Coding","text":"<p>With the increasing energy bills not only due to inflation but energy becoming more expensive resource, it is more important to consider the settings of our thermostats to lower our bills. The tolerance (mentioned in the how-to page) if higher, can provide an approach to lower energy bills. </p> <p>It is also believed that, when away from home, if the HVAC is turned up by a few degrees it is possible to lower energy cost, specifically in the summer. Now, a simple solution could be to manually increase temperature each day and lower temperature each night. However, a much more efficient solution would be to program this action for specific times, say raising the temperature at 10 AM by 8 degrees Fahrenheit and lowering back the temperature at 8 PM, or one could also set the times to those of the beginning of peak hour and the end. </p>"},{"location":"have_digits/","title":"Have Digits","text":"<p>This function checks if there is a digit in the string</p> <p>Outputs 1 as long as there is any digit in the string</p> <p>Parameters:</p> Name Type Description Default <code>s</code> <code>str</code> <p>a string with or without digits</p> required <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>1 or 0</p> Source code in <code>example_functions.py</code> <pre><code>def have_digits(s: str) -&gt; int:\n    \"\"\"This function checks if there is a digit in the string\n\n    Outputs 1 as long as there is any digit in the string\n\n    Args:\n        s (str): a string with or without digits\n\n    Returns:\n        int: 1 or 0\n    \"\"\"\n\n    out = 0\n\n    # loop through the string\n    for c in s:\n        # check if the character is a digit\n        if c.isdigit():\n            out = 1\n            break\n\n    return out\n</code></pre>"},{"location":"how_to_thermo/","title":"Themo Stat Function How-to Make Changes","text":""},{"location":"how_to_thermo/#changing-the-tolerance","title":"Changing the Tolerance","text":"<p>Follow the tutorial to get the starter function. This is only a very simple starting function, we can stop here if we want but why do so when we can improve it. There are many avenues to altering this function, but let's begin with a simple change that gets us more familiar with this function. </p> <p>We can make a small alteration by changing the value of difference required between the two variables <code>temp</code> and <code>desired_temp</code> by defining a new variable called <code>tol = 5</code> and replacing it with the <code>5</code> in</p> <pre><code>if temp &lt; desired_temp - 5:\n</code></pre> <p>and </p> <pre><code>elif temp &gt; desired_temp + 5:\n</code></pre> <p>If this variable is set to <code>tol = 5</code> and the tests above are rerun, you should get the same exact outcome. But you can set this to any other number now. Note: instantiate this variable before the if statements.</p> <p>Though this is a simple change, it allows us to probe the function and learn how we can begin to improve it for a more complex version. You now have the starting function and have made the first change, these are the tools you can use for further development. Good luck!</p>"},{"location":"perimeter/","title":"Perimeter of Rectangle","text":"<p>This function returns the perimeter of a rectangle with a width and height</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>int | float</code> <p>width of the rectangle</p> required <code>height</code> <code>int | float</code> <p>height of the rectangle</p> required <p>Returns:</p> Type Description <code>int | float</code> <p>int | float: perimeter of the rectangle</p> Source code in <code>example_functions.py</code> <pre><code>def perimeter_of_rectangle(width: int | float,height: int | float) -&gt; int | float:\n    \"\"\"This function returns the perimeter of a rectangle with a width and height\n\n    Args:\n        width (int | float): width of the rectangle\n        height (int | float): height of the rectangle\n\n    Returns:\n        int | float: perimeter of the rectangle\n    \"\"\"\n    perimeter = 2*(width+height)\n    return perimeter\n</code></pre>"},{"location":"thermo_status/","title":"Thermo Stat","text":"<p>This function returns the status from three options based on the current and desired temperature </p> <p>Parameters:</p> Name Type Description Default <code>temp</code> <code>int or float</code> <p>the current temperature</p> required <code>desired_temp</code> <code>int or float</code> <p>the desired temperature</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>status of any change required or not</p> Source code in <code>example_functions.py</code> <pre><code>def my_thermo_stat(temp: int | float, desired_temp: int | float) -&gt; str: \n    \"\"\"This function returns the status from three options based on the current and desired temperature \n\n    Args:\n        temp (int or float): the current temperature\n        desired_temp (int or float): the desired temperature\n\n    Returns:\n        str: status of any change required or not\n    \"\"\"\n    if temp &lt; desired_temp - 5:\n        status = 'Heat'\n    elif temp &gt; desired_temp + 5:\n        status = 'AC'\n    else:\n        status = 'off'\n    return status\n</code></pre>"},{"location":"tutorial_thermo/","title":"Themo Stat Function Tutorial","text":"<p>This is a tutorial of how the <code>thermo stat</code> function can be used to check the status of a thermostat depending on the current and desired temperature. Below lies an example of how to use this function. </p>"},{"location":"tutorial_thermo/#getting-started","title":"Getting Started","text":"<p>First we begin by creating a python file <code>test_func.py</code> and within it, you can begin by copying the following code:</p> <pre><code>    def my_thermo_stat(temp: int | float, desired_temp: int | float) -&gt; str: \n\n        if temp &lt; desired_temp - 5:\n            status = 'Heat'\n        elif temp &gt; desired_temp + 5:\n            status = 'AC'\n        else:\n            status = 'off'\n        return status\n</code></pre> <p>Then, we can check if this code runs by calling the function with the following test inputs </p> <p>Input:</p> <pre><code>my_thermo_stat(25,25)\nmy_thermo_stat(25,19)\nmy_thermo_stat(15,22)\n</code></pre> <p>Output:</p> <pre><code>'off'\n'AC'\n'Heat'\n</code></pre> <p>For consistency with these tests, it is important to copy the function exactly as outlined above. This function only intended to provide the status of the thermostat to reach the desired temperature, and does not perform any additional action. </p>"}]}